============
Installation
============

At the command line::

    $ pip install nuage-openstack-neutron

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nuage-openstack-neutron
    $ pip install nuage-openstack-neutron
